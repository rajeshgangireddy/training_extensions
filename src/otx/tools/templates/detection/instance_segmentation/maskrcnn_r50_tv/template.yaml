# Description.
model_template_id: Custom_Instance_Segmentation_MaskRCNN_ResNet50_v2
name: Mask R-CNN ResNet-50 v2
task_type: INSTANCE_SEGMENTATION
task_family: VISION
instantiation: "CLASS"
summary: Instance Segmentation using Mask R-CNN ResNet-50 v2 with an Enhanced Bounding Box Head
application: ~

# Algo backend.
framework: OTXDetection v2.9.1

# Capabilities.
capabilities:
  - compute_representations

# Hyperparameters.
hyper_parameters:
  base_path: ../configuration.yaml
  parameter_overrides:
    learning_parameters:
      batch_size:
        default_value: 4
      inference_batch_size:
        default_value: 1
      learning_rate:
        default_value: 0.007
      num_iters:
        default_value: 100

# Training resources.
max_nodes: 1
training_targets:
  - GPU
  - CPU

# Stats.
gigaflops: 560.5
size: 177

# Model spec
model_category: BALANCE
